def flip_vertical_axis(matrix):
    for x in range(len(matrix)):
        matrix[x] = matrix[x][::-1]
        
"""
def flip_vertical_axis(matrix):
    for row in range(len(matrix)):
        temp = matrix[row]
        temp = temp[::-1]
        matrix[row] = temp
        
  Repractice
"""
